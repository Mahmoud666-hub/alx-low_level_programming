#include "main.h"
/**
 * _abs - tests whether a character is a
 * lowercase letter from the English alphabet.
 * @c: character to test.
 * Return: 1 if it is a lowercase letter, 0 if it is not a lowercase letter.
 */
int _abs(int c)
{
	int n;

	if (c < 0)
	{
		n = (-1) * n;
	} return (n);
}
