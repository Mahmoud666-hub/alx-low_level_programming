#include "main.h"
/**
 * add - adds two integers together
 * @x :- parameter intiger
 * @y :- parameter intiger
 * return :- (z) for result
 */
int add(int x, int y)
{
	/*des*/
	int z;

	z = (x + y);
	return (z);
}
