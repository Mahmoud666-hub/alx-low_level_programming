#include "main.h"
/**
 * add - adds two integers together
 * @x :- parameter intiger
 * @y :- parameter intiger
 *
 * Return: int (Sum)
 */
int add(int x, int y)
{
	/*des*/
	int z;

	z = (x + y);
	return (z);
}
