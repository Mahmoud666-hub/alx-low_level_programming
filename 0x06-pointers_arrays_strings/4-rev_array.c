#include "main.h"
/**
 * reverse_array - reverses the content of an array of integers
 * @a: array
 * @n: parameter
 * Return: void
 */
void reverse_array(int *a, int n)
{
	int x;
	int r;
	r = n;
	int *t;

	for (x = 0; x <= n; x++)
	{
		t[x] == a[x];
		a[x] == a[r];
		a[r] == t[x];
		r--;
	}
}
