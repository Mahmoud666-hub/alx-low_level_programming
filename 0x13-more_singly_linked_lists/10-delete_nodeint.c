#include"lists.h"
/***/
int delete_nodeint_at_index(listint_t **head, unsigned int index)
{
unsigned int x = 0;
listint_t *p = *head;
listint_t *u = *head;
if ((*head) == NULL || head == NULL)
{
return (-1);
}

}
