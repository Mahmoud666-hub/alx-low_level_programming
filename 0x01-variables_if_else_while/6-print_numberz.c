#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	/*describbe*/
	int v;

	for (v = 48; v <= 57; v++)
	{
		putchar(v);
	}
	putchar('\n');
	return (0);
}
