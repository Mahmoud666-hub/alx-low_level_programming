#include<stdio.h>
#include<string.h>
char *s;
void main()
{
	
	char *haystack = "First, solve the problem. Then, write the code.";
	char *needle = "";
//printf("%s\n", strstr(haystack, needle));
printf("%s\n", needle);
}

